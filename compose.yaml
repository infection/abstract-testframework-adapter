services:
    zizmor:
        image: ghcr.io/zizmorcore/zizmor:1.29.0
        environment:
            GH_TOKEN: ${GH_TOKEN:-}
        working_dir: /workspace
        volumes:
            - .:/workspace:ro
